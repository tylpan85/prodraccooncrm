// Phase 9 fills this package with the ported recurrence engine
// from the legacy beta. Keep exports stable from day one so
// dependent modules can wire against them incrementally.

export const RECURRENCE_ENGINE_VERSION = '0.0.0-stub';
