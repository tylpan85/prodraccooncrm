// Phase 1: populate with `Raccoon Cleaning Inc` org + admin + team + services.
async function seed() {
  console.log('[seed] placeholder — Phase 1 implements this');
}

seed().catch((err) => {
  console.error(err);
  process.exit(1);
});
